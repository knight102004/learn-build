# learn-build
Author- Jubaid Ali Halder ,a B.Tech  CSE student
<br> Here i am writing down by git and github learning journey

<br>
git config --global user.name "My Name"
→ Sets the name that Git uses for your commits.

<br>

git config --global user.email "email@gmail.com"
→ Sets the email associated with your Git/GitHub account and used for your commits.

<br>

git config --global --list
→ Displays the global Git configuration, including the name and email you just entered.

<br>
cd - change directory 
move to the next inside folder

clone command 
git clone https://github.com/knight102004/learn-build.git
it completely clones the all document in that repo to the vs code

works on git bash not on cmd
ls - list all files
ls -a - list all files including hidden


1)git status
tells about if it is changed or requires commit 

1.1)untracked file -
 file that git doesn't yet track

1.2)staged- if we add a change 
1.3)modified- we have changed something in the file 
1.4)unmodified - we havn't done any change   it is called unmodified

 git status
change(modified)/new file(untracked)-->add(staged)-->commit(unchanged)

we use "git add" to apply for the change and add into git 
